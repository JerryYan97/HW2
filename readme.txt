1): Add a pair of shoes;
2): Realize the function that:
"Connect signals and slots in your GUI such that when a 
particular type of node is selected in your tree widget, 
only the transformation adjustment widgets that are compatible 
with that node are 'active'."